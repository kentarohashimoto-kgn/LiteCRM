-- =====================================================================
-- AI-PMO バッチを毎晩実行 → 週1回(日曜23時JST)に変更（API料金節約）
--   vercel.json の cron を "0 16 * * *" → "0 14 * * 0" に変更したのに合わせ、
--   AIバッチ運用画面(/app/exec/batch)に表示されるラベル/説明を更新する。
--   job_kind('pmo_nightly')は実行ログ(batch_runs)との連続性のため変更しない。
-- =====================================================================

update public.batch_job_settings
set
  label = 'AI-PMO週次レポート（日曜23時）',
  description = 'ベテランPMアドバイザーの4モード(振り返りPDCA/未来段取り/案件PJ管理/経営俯瞰)を毎週日曜夜に自動生成。月曜朝イチにAI-PMO画面で最新レポートが読める。平日に必要な場合はAI-PMO画面から手動生成する。'
where tenant_id = '00000000-0000-0000-0000-000000000001'
  and job_kind = 'pmo_nightly';
