-- ============================================================
-- 0196: 候補者一覧の表示拡充にともなう年齢カラム追加
-- 一覧・詳細・求人詳細で 稼働量(desired_workload)・単価(desired_pay)・
-- 年齢(age)・人事コメント(notes) を表示する。未保持だった年齢のみ新設し、
-- 稼働量/単価/人事コメントは既存カラムを流用する。
-- ============================================================

alter table public.candidates
  add column if not exists age int;  -- 年齢

-- ============================================================
-- ロールバック:
--   alter table public.candidates drop column if exists age;
-- ============================================================
