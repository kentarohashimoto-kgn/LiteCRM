-- ============================================================
-- タスクに参照URLを追加
--   - tasks.url: タスクに紐づく参照リンク（資料/ドキュメント/関連ページ等）。
--     NULL 可。説明(description)は既存カラムを利用する。
-- ============================================================

alter table public.tasks add column if not exists url text;
